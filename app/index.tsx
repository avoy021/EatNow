import { Onboarding } from "@/components";
import { SafeAreaView } from "react-native-safe-area-context";
 
const Home = () => {
    return (
        <SafeAreaView>
            <Onboarding />
        </SafeAreaView>
    )
}

export default Home;